export const BASE_URL =
  location.hostname === "localhost"
    ? "http://localhost:3000"
    : "https://campfire-cove-resort-booking-back-end.vercel.app";

// export const BASE_URL =
//   location.hostname === "localhost" ? "http://localhost:3000" : "/api";
